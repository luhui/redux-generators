import { createAction } from 'redux-actions'
import { createAsyncActions } from 'redux-async-actions'
import { createAsyncTypes } from 'redux-async-types'

const types = createAsyncTypes(<%=prefix%>,
  'YOUR_ACTION'
)

export const testAction = createAction(YOUR_ACTION)
